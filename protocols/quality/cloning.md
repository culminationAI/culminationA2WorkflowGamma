# Cloning Protocol

## Overview

Reproduces the coordinator as a blank-but-self-aware instance in a new workspace. The clone receives the full evolved brain (protocols, agents, memory scripts, MCP templates) but goes through initialization fresh — new name, new project context, new user identity.

This is NOT variant testing (see `protocols/core/build-up.md` Steps 3-4 for worktree-based variant testing).

## Triggers

- User command: `/clone [target]`
- Manual: `python3 memory/scripts/clone.py [target]`

## Vocabulary

- **Source** — the running coordinator workspace
- **Clone** — the new coordinator instance being created
- **Brain files** — transferable coordinator knowledge (protocols, agents, scripts)
- **Runtime data** — non-transferable state (memories, secrets, user identity, logs)

## Brain vs Runtime

### Cloned (brain files)

```
CLAUDE.md                                    (transformed — see Step 4)
.claude/agents/pathfinder.md                 (4 base agents)
.claude/agents/engineer.md
.claude/agents/protocol-manager.md
.claude/agents/llm-engineer.md
protocols/core/                              (all core protocols)
protocols/agents/                            (all agent protocols, accord → template)
protocols/knowledge/                         (all knowledge protocols)
protocols/quality/                           (all quality protocols, including this one)
protocols/README.md
memory/scripts/                              (all Python scripts)
mcp/mcp-full.json                            (portable — uses __WORKSPACE_ROOT__)
mcp/mcp_configure.py
mcp/ollama_qdrant_server.py
mcp/with-env.sh
infra/docker-compose.yml
infra/exchange-shared/                       (shared exchange server)
infra/responder/                             (live responder)
docs/self-architecture/capability-map.md     (regenerated on init Phase 10)
docs/self-architecture/build-registry.json   (reset to init-001 stub)
docs/self-architecture/spec-registry.json    (accumulated universal specs)
secrets/.env.template
setup.sh, install.sh, .gitignore, README.md
```

### NOT Cloned (runtime data)

```
user-identity.md          — created fresh during init Phase 2
secrets/.env               — user populates manually
protocols/project/         — created during init Phase 5
domain agents              — created during init Phase 5
mcp/mcp.json               — hardcoded paths, regenerated by mcp_configure.py
infra/neo4j_data/          — live graph data
infra/qdrant_storage/      — live vector data
infra/ollama_models/       — downloaded model weights
docs/self-architecture/meditation-log.md
docs/self-architecture/gap-analysis-log.md
docs/exploration-report.md
docs/research/
docs/specs/
logs/
evolve/
.session_lock
```

## Cloning Modes

### Mode A: GitHub Repository

Target format: `owner/repo-name`

Creates a new private GitHub repo, pushes the brain as initial commit. Requires `gh` CLI authenticated.

```bash
python3 memory/scripts/clone.py owner/repo-name
```

### Mode B: Local Directory

Target format: `/path/to/directory`

Copies brain files to a local directory. Optional `--init-git` to initialize a git repo.

```bash
python3 memory/scripts/clone.py /path/to/directory
python3 memory/scripts/clone.py /path/to/directory --init-git
```

## Process

### Step 1: Pre-Flight Check

- Verify `git` is available
- For GitHub mode: verify `gh` CLI is installed and authenticated
- Verify target path/repo doesn't already exist
- Verify source workspace is clean (no critical files missing)

### Step 2: Resolve Target

Detect mode from target string:
- Contains `/` but does NOT start with `/` → GitHub (`owner/repo`)
- Starts with `/` or `.` → local directory

### Step 3: Assemble Brain Files

Collect all files from the include list (see Brain vs Runtime above). Skip files that don't exist in source. For directories, collect recursively.

### Step 4: Apply Transformations

Three files are modified before writing to clone:

**CLAUDE.md:**
- Insert `<!-- _WORKFLOW_NEEDS_INIT -->` marker before `## Role`
- Keep existing `<!-- WORKFLOW_VERSION: X.XX -->` (brain maturity indicator)
- The clone's initialization Phase 2 Step 3 will assign a new name

**build-registry.json:**
- Replace with minimal stub: 1 entry, type=initialization, status=pending
- Records clone origin: "Brain imported from coordinator vX.XX"

**knowledge-exchange-accord.md:**
- Status: ACTIVE → Status: TEMPLATE
- Parties: cleared (TBD placeholders)
- Template notice appended: "This instance has not entered any bilateral accord"
- All principles and operational sections preserved as reference

### Step 5: Write to Target

Copy all collected files to target, applying transformations. Create parent directories as needed. Create empty `protocols/project/.gitkeep` for project-specific protocols.

### Step 6: Post-Clone Setup

**GitHub mode:**
```bash
git init && git add . && git commit -m "chore: initial brain import from coordinator vX.XX"
gh repo create owner/repo --private --source . --push
```

**Local mode (with --init-git):**
```bash
git init && git add . && git commit -m "chore: initial brain import from coordinator vX.XX"
```

Print next steps:
```
Clone created at: {target}
Next: Open the directory in Claude Code.
      The coordinator will detect _WORKFLOW_NEEDS_INIT and self-initialize (11 phases).
```

### Step 7: Verify

Automated checks (8 total):

1. CLAUDE.md contains `_WORKFLOW_NEEDS_INIT` marker
2. CLAUDE.md does NOT contain the marker twice (idempotency)
3. `mcp/mcp-full.json` exists and contains `__WORKSPACE_ROOT__`
4. `mcp/mcp.json` does NOT exist
5. `secrets/.env` does NOT exist
6. `user-identity.md` does NOT exist
7. `knowledge-exchange-accord.md` contains `TEMPLATE`
8. `build-registry.json` has exactly 1 entry of type `initialization`

## What Happens When the Clone Runs

1. Clone opens in Claude Code → CLAUDE.md loaded → `_WORKFLOW_NEEDS_INIT` detected
2. Initialization protocol runs all 11 phases
3. Phase 2: clone gets a NEW name (generated via `generate_name.py`)
4. WORKFLOW_VERSION stays at source level (e.g. 2.35) — brain maturity preserved
5. Phase 10: version NOT reset to 1.0 (guard: only 0.2 → 1.0 for fresh installs)
6. After init: clone is a fully autonomous coordinator with its own identity

## Dry Run

Preview what would be cloned without writing anything:

```bash
python3 memory/scripts/clone.py target --dry-run
```

Shows file list, marks transformations with `[TRANSFORM]`.

## Constraints

- Clone gets a NEW name — never inherits source coordinator's name
- Bilateral accords (knowledge-exchange-accord) become templates, not active agreements
- `mcp/mcp.json` never copied — contains hardcoded absolute paths
- Secrets never copied — user configures manually
- Target must not already exist (no overwrite)

## Automation

Script: `memory/scripts/clone.py`

## See Also

- `protocols/core/initialization.md` — the onboarding process the clone runs
- `protocols/core/build-up.md` Steps 3-4 — worktree-based variant testing (different from cloning)
- `protocols/quality/testing.md` — testing framework for build-up variants
